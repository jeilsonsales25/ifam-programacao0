/*Faça um programa que leia n números, some-os e exiba uma mensagem com o
resultado apenas se o resultado for maior que 100.*/
let quantidade = parseInt(prompt("Quantos números?"))
var i = 0
const numeros = []
var soma = 0
soma += numeros[i]

for(let i = 0; i<quantidade; i++)
    numeros[i]= prompt()
    i++
    if(soma>100)
    alert(`Valor: ${soma}`)
